import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.akfakf0509.shotexpense',
  appName: '찰칵가계부',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
